# Railway production: use the PostgreSQL service DATABASE_URL value.
DATABASE_URL=postgresql://user:password@host:5432/barun_travel
DATABASE_SSL=true
ADMIN_INITIAL_USERNAME=admin
# Use a unique long password. This only creates the first account.
ADMIN_INITIAL_PASSWORD=replace-with-a-strong-password
NODE_ENV=production
