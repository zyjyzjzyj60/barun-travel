# 巴伦旅游（바른투어）项目要求

## 1. 项目目标与边界

- 本站是面向**韩国旅客来中国旅游**的公开演示与预订平台，品牌为巴伦旅游（바른투어）。
- 首轮升级的交付目标是“西安 3 晚 4 日”和“丝绸之路 11 日”两条路线完整上线；在此完成前，不得将它们表述为已具备同等功能。新增路线必须沿用相同的产品、团期、行程与图片数据结构，不为单一产品写死逻辑。
- 当前是演示站：可以提交测试订单与完成“测试支付”，但不得接入真实收款、真实 KakaoPay、退款或银行卡处理。
- 不收集、不保存护照号、证件照片等敏感旅客资料；联系人信息只用于测试订单演示。

## 2. 内容与体验要求

- 目标用户是韩国消费者：页面以**韩文为主、中文为辅助对照**。导航、表单和行动按钮以韩文为准；路线、景点、费用与订单透明度信息提供中韩对照，避免为了双语而让每个控件重复两遍。
- UI 不复制任何参考旅行网站。设计方向为“电影感探索”：高质量景点影像、克制的深色基调、中国文化色彩点缀、清晰的预订动作和移动端优先布局。
- 每个产品详情页必须包含：路线亮点、费用说明、可售团期、人数与总价计算、测试预订入口、轻量旅程地图，以及逐日行程。
- 每一天必须说明当天去哪里、主要交通或转场、餐食/住宿（有资料时）和重点景点。重点景点以图文卡片展示，包含景点名称、中韩介绍、图片和必要的停留/交通提示；接送机、入住等非景点活动可简洁呈现。
- 只展示后台实际可售的团期、价格和余位；演示团期与演示价格必须清晰标记，不得伪造实时库存、客户评价、历史销量或保障承诺。

## 3. 图片与内容授权

- 景点图片优先使用 Wikimedia Commons 或其他授权清晰、可商用的来源；下载并随站点部署，禁止热链第三方旅行网站图片。
- 每张上线图片必须保存景点名称、作者、授权协议、授权链接和原始来源，并在 `public/attribution.html` 可追溯展示。
- 图片须生成适合 Web 使用的多尺寸 WebP 资源；保留原始授权信息，不以未经授权的图片临时替代。
- 任何仍可从站内访问的页面都不得保留第三方景点图片热链；若图片经裁切、压缩或其他改编，应按原协议保留署名并履行相应的同许可要求。
- 行程、景点、签证、航班、价格和政策等内容必须可核实。不能确认的事实应标注为待确认，不得编造。

## 4. 技术与安全基线

- 目标生产技术栈为 Node.js 18+ 原生 HTTP 服务、PostgreSQL（`pg`）和静态前端；本地演示可用 `pg-mem`，但内存数据不能视为持久数据。
- 生产环境必须从 `DATABASE_URL` 读取 PostgreSQL 连接；仅在本地或 Railway 演示兜底时使用内存数据库。
- 服务端必须使用 `process.env.PORT || 3023`，并保持 `npm start` 可直接启动。
- 管理员账号仅由 `ADMIN_INITIAL_USERNAME` 和 `ADMIN_INITIAL_PASSWORD` 初始化；密码必须哈希存储，严禁在源码、前端或仓库中写入真实账号、密码、密钥或支付凭证。
- 后台接口必须校验会话和输入；订单金额、余位和订单状态只在服务端计算及更新。
- 测试支付必须明显标记为“演示/测试支付”，订单记录保持 `is_test=true` 或等价标记。
- 轻量旅程地图使用站内 SVG/CSS/数据渲染，不依赖地图密钥、第三方跟踪脚本或外部地图服务。

## 5. 数据、改动与发布规范

- 产品、每日行程、重点景点、图片授权信息、团期和订单数据应使用标准化结构。修改任一路线时不得影响另一条路线的数据或预订流程。
- 所有 JSON 和文本文件使用 UTF-8 无 BOM 编码；在 Windows 上修改 JSON 时使用 Node.js 或编辑器，避免 PowerShell 5.1 生成 BOM。
- 不删除或覆盖用户已有的未提交文件、部署配置、订单数据或图片资产，除非任务明确授权且已确认目标。
- 修改后至少验证：两条产品可打开、每日行程完整、图片与授权页可加载、团期/人数计价正常、测试订单与后台订单查看正常、手机宽度布局可用；核心价格、库存和订单接口需有可运行的 `node --test` 覆盖，不能长期保持零测试。
- 发布使用 Git 保留目录结构并推送至现有 GitHub 仓库，再由 Railway 构建。部署后必须在公网 HTTPS 地址复测页面、API、图片和测试下单；没有 PostgreSQL 时需明确告知订单数据会在重启后丢失。

## 6. 后续功能优先级

1. 完成两条路线的高质量中韩双语逐日图文与可授权景点图片。
2. 将前台、团期、订单和后台逻辑全面泛化为多产品支持。
3. 接入持久化 PostgreSQL 后再进行稳定公网演示。
4. 仅在公司完成 KakaoPay/PG 商户签约、隐私与退款流程准备后，评估真实支付、旅客资料与通知能力。
